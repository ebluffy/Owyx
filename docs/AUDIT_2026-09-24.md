# Owyx full audit — 2026-09-24

**Repo:** [ebluffy/Owyx](https://github.com/ebluffy/Owyx) (fork of [modrinth/code](https://github.com/modrinth/code))  
**HEAD:** [`1f421de75`](https://github.com/ebluffy/Owyx/commit/1f421de75) `chore: bump launcher version to 0.10.0`  
**Prior umbrellas:** [#108](https://github.com/ebluffy/Owyx/issues/108), [#117](https://github.com/ebluffy/Owyx/issues/117), [#126](https://github.com/ebluffy/Owyx/issues/126), [#133](https://github.com/ebluffy/Owyx/issues/133) — issues **#41–#136 CLOSED**. This pass does **not** reopen them unless the same bug is still true.

**Scope:** security, correctness, UX, i18n, CI, contracts, Modrinth leftovers, telemetry, Social / Servers / skins.  
**This PR:** docs + inventory only. Fixing every finding is **out of scope**.  
**Umbrella issue:** [#142](https://github.com/ebluffy/Owyx/issues/142) · **PR:** [#141](https://github.com/ebluffy/Owyx/pull/141)

Safe upstream playbook (same PR): [`docs/UPSTREAM_SYNC.md`](./UPSTREAM_SYNC.md).

## Skills

- Catalog: [ebluffy/agent-tool-catalog](https://github.com/ebluffy/agent-tool-catalog)
- mattpocock: **code-review** (standards vs spec of #127–#136 / PR #140), **diagnosing-bugs** (CI ClickHouse, Mojang gate, friends join)
- addyosmani: security / CI (JWT-in-query, lockout, token scopes, compose health)
- hallmark + ui-ux-pro-max through **Owyx** `brand/DESIGN.md` first (cyan `#00e5ff`, bg `#050508`, RU/EN parity)

---

## 1. Past themes — FIXED vs still open

Owner-requested recheck on `1f421de75`. Only file what is **still true**.

| Theme | Verdict | Evidence | Residual |
|-------|---------|----------|----------|
| Resource-id log spam (`The resource id … is invalid`) | **FIXED** | Client drop: `apps/app-frontend/src/helpers/error-reporting.ts` `isNoisy`; send gate `owyx-telemetry.ts`; ingest `activityLog.js` + `launcher.js` 202 on all-noise; updater RID soft-fail `apps/app/src/updater_impl.rs`; purge SQL `owyxsite/postgres/scripts/purge-resource-id-telemetry.sql` | Historical rows remain until the purge script is run on prod. Old launcher builds can still POST until upgraded. |
| Win11 shown as `10.0.xxxxx` | **FIXED** (UI) | `owyx-os-label.ts` build ≥ 22000 → `Windows 11 (build N)`; tests `owyx-os-label.test.ts`; About `AppSettingsModal.vue`; telemetry **display** name uses the helper | Telemetry field `os` is the *label*, not an enum (`windows`). Raw `osVersion` still `10.0.xxxxx`. See P2 below. |
| Friends `Playing owyx-server:…` | **FIXED** (UI) | `FriendsList.vue` `friendPlayingLabel` hides the slug (#136) | **Copy** still writes raw `instanceName` (P2). Catalog miss → “unknown instance”, not the slug. |
| Server packs hidden from Library | **FIXED** | #127 asked to **show** them again. `use-library.ts` has no `isOwyxServerInstance` filter; packs install under `profiles/servers/` and appear in Library | Helper `isOwyxServerInstance` is unused for filtering (correct after #127). |
| Owyx Servers Settings modal | **FIXED** | `OwyxServers.vue` `openServerSettings` → `InstanceSettingsModal` (#128) | Rare race: `settingsModalNotReady` if modal has not mounted — retry. |
| Owyx skin selector | **PARTIAL** | Upload `owyx-skin-upload.ts` (`PUT /api/profile/skin`); apply path in `Skins.vue` for offline + site session (#129) | `isSkinManagementReadOnly` still keys off Mojang `check_reachable()` — blocks Owyx apply when sessionserver.mojang.com is down (P1). |
| Pack ACL / cache | **FIXED** (current deploy) | `packUploadAcl.js` + `optionalAuthenticate`; `server.ts` `Cache-Control: private, no-store`; `deploy/nginx-owyx.site.conf` `^~ /uploads/packs/` | Confirm VPS uses **this** nginx file. Download route sets `Content-Disposition` only. |
| Discord OAuth `state` | **FIXED** | `auth.js` random state + `timingSafeEqual`; HMAC cookie `discordPendingCookie.js` | Query-string JWT on **start** is a leftover landmine (P1). |
| API tokens on password change | **FIXED** | `revokeUserCredentials` on profile password, reset, email confirm; auth requires `is_active` | Rows are deactivated, not deleted. |
| CI prettier / clippy `sort_by` (#118 / #130) | **FIXED** in source | `utils.rs` uses `sort_by_key`; last full lint stack green on merge #140 ([35916673675](https://github.com/ebluffy/Owyx/actions/runs/35916673675)) | `1f421de75` turbo-ci **died before lint** (ClickHouse). P1. |

---

## 2. New findings (still true on HEAD)

**P0:** none (no live RCE / unauthenticated pack ACL bypass / secret in repo).

### P1

| ID | Issue | Title | Paths | Why | Fix hint |
|----|-------|-------|-----|----------|----------|
| A | [#143](https://github.com/ebluffy/Owyx/issues/143) | Friends **Join** skips `requiresAccount` | `apps/app-frontend/src/components/ui/friends/FriendsList.vue` `joinFriendServer` (~383–407) vs `OwyxServers.vue` `playServer` (~246–250) | Catalog `requiresAccount` is a product gate. Play prompts Owyx sign-in; Join from friends installs the pack and launches anyway. | Mirror `playServer`: if `server.requiresAccount && !owyx.isSignedIn`, `signIn()` and abort on failure. |
| B | [#144](https://github.com/ebluffy/Owyx/issues/144) | Owyx skin apply blocked by Mojang reachability | `apps/app-frontend/src/pages/Skins.vue` `isSkinManagementReadOnly` (~390–394), `authServerQuery` (~312–316); `packages/app-lib` `check_reachable()` | Read-only is `navigator.onLine` **or** `sessionserver.mojang.com` error. Correct for Microsoft accounts; wrong for Owyx offline + site session (`PUT /api/profile/skin` does not need Mojang). | For `isOfflineAccount && owyxSite.isSignedIn`, gate only on `navigator.onLine` (optional Owyx `/me` ping). |
| C | [#145](https://github.com/ebluffy/Owyx/issues/145) | JWT accepted in query string (Discord start + Socket.IO) | `owyxsite/backend/src/routes/auth.js` `resolveDiscordStartUser` (~758–763); `owyxsite/backend/src/socket/index.ts` (~36–38); `server.ts` `morgan('combined')` | Bearer in `?token=` lands in access logs, history, Referer. Combined format logs the full URL. Current profile UI does not start Discord this way; chat page is Coming Soon — the **API still accepts it**. | Drop query auth. Discord: Authorization or a one-time cookie then redirect. Socket.IO: `handshake.auth.token` only. Redact `token` in Morgan. |
| D | [#146](https://github.com/ebluffy/Owyx/issues/146) | `main` turbo-ci red: ClickHouse unhealthy | `.github/workflows/turbo-ci.yml` “Start services”; `docker-compose.yml` `clickhouse` healthcheck (3×3s) | Run [35916969381](https://github.com/ebluffy/Owyx/actions/runs/35916969381) on `1f421de75` died at `docker compose --profile clustered-redis up --wait` (`labrinth-clickhouse is unhealthy`). Lint / clippy / prettier / intl **did not run** on the 0.10.0 bump. Fork has no Namespace cache; cache miss → full Labrinth stack on `ubuntu-latest`. | Lengthen ClickHouse `start_period` / retries; retry the compose step; or skip Labrinth compose on this fork when only app/site paths change. |

### P2

| ID | Issue | Title | Paths | Why | Fix hint |
|----|-------|-------|-----|----------|----------|
| E | [#147](https://github.com/ebluffy/Owyx/issues/147) | Copy “what they’re playing” copies `owyx-server:<id>` | `FriendsList.vue` `copyPlayingInstance` (~332–345) vs `friendPlayingLabel` (~247–255) | UI hid the slug (#136); clipboard did not. | Copy `friendPlayingLabel(friend)` or catalog name. |
| F | [#148](https://github.com/ebluffy/Owyx/issues/148) | Telemetry `os` is a display label | `owyx-telemetry.ts` (~96–99); contract example `"os": "windows"` | Admin queries expecting an enum get `Windows 11 (build 26200)`. | Send `os: platform()` lowercase; put the pretty string in `metadata.osLabel`. |
| G | [#149](https://github.com/ebluffy/Owyx/issues/149) | Account-wide login lockout (DoS) | `owyxsite/backend/src/routes/auth.js` `checkLoginAttempts` (~245–261, ~481–486) | Five failed logins for a **login identifier** (any IP) lock the account for 1 hour. Attacker who knows email/nick can lock out the owner. Launcher path skips Turnstile. | Split per-(IP, identifier) vs global; captcha / step-up instead of hard lock; admin unlock. |
| H | [#150](https://github.com/ebluffy/Owyx/issues/150) | Long-term API token `permissions` never enforced | `auth.js` `attachLongTermApiToken` (~163–177) | `req.apiToken.permissions` is stored and ignored. A token on an admin user is full admin. Previously deferred in #133; still true and user-facing on `/tokens`. | Enforce scopes per route, or drop the field and document “full account role”. |
| I | [#151](https://github.com/ebluffy/Owyx/issues/151) | Forum create + resend-verification unbounded | `forum.js` `POST /topics`; `auth.js` `POST /send-verification` | Auth-only, no per-user throttle → SMTP / DB spam. | `consumeIp` / per-user buckets like register. |
| J | [#152](https://github.com/ebluffy/Owyx/issues/152) | ru-RU missing 61 upstream keys | `apps/app-frontend/src/locales/ru-RU/index.json` vs `en-US` | `owyx.*` / skins / friends / servers: **parity**. Gaps are download-manager, resource-management store, bulk selection. CI does not check en⊆ru. | Translate or hide those surfaces until translated; optional CI prefix check. |
| K | [#153](https://github.com/ebluffy/Owyx/issues/153) | Crowdin push fails on every locale change | `.github/workflows/i18n-push.yml` | Missing `CROWDIN_PROJECT_ID` / `CROWDIN_PERSONAL_TOKEN` → red check on `main`. | `if:` skip when secrets unset. |
| L | [#154](https://github.com/ebluffy/Owyx/issues/154) | No CI for `owyxsite/` | `.github/workflows/*` | Site/API regressions do not block merges. | Add a job: `node` tests (`*.test.js`) + lint on `owyxsite/backend`. |
| M | [#155](https://github.com/ebluffy/Owyx/issues/155) | Shared-instance invites still `plugin:friends` | `use-shared-instance-invite-candidates.ts` → `use-friends.ts` → `helpers/friends.ts` `invoke('plugin:friends|…')` | Contract: Social is Owyx `/api/friends`. Owyx-only users get empty/wrong invite lists; copy still says Modrinth. | Hide Share for Owyx-only builds **or** rewire candidates to `owyx-friends.ts`. |
| N | [#156](https://github.com/ebluffy/Owyx/issues/156) | `~/owyx/site_session.json` default umask | `apps/app/src/api/utils.rs` `owyx_site_session_set` (~61–68) | JWT left the webview (good) but file is often world-readable on Linux. | `0o600` after write on Unix. |
| O | [#157](https://github.com/ebluffy/Owyx/issues/157) | Launcher never reads `serverAccess` | `owyx-site-auth.ts` `fetchOwyxSiteMe`; contract § login step 4 | `/me` returns `serverAccess` / `accessReason`. Client stores user/cosmetics only. **Server already 403s banned sessions** in `authenticateToken` — this is UX + stale-cache play, not an auth bypass. | Persist the flags; gate Play/Join; show localized `banned` / `inactive`. |
| P | [#158](https://github.com/ebluffy/Owyx/issues/158) | Tauri CSP still allowlists PostHog / Sentry | `apps/app/tauri.conf.json` | Runtime does not init them; CSP still permits the origins. | Tighten to Owyx + Mojang + GitHub Releases. |

---

## 3. Cross-checks (high-risk list from #133, still hold)

| Area | State @ `1f421de75` |
|------|---------------------|
| tombi / typos | check-generic **success** ([35916969328](https://github.com/ebluffy/Owyx/actions/runs/35916969328)) |
| cargo shear | check-rust **success** ([35916969340](https://github.com/ebluffy/Owyx/actions/runs/35916969340)) |
| prettier / eslint / clippy / intl | Proven on #140 merge, **not re-run** on version bump (ClickHouse) |
| API tokens (#99) | revoked on credential change |
| Discord `state` (#100) | cookie + `timingSafeEqual` |
| Pack ACL (#120) + cache (#131) | middleware + nginx `private, no-store` |
| Ingest cap (#132) | contract and `MAX_PACK_BYTES` = 512 MB; client stream cap matches |
| Sentry / PostHog runtime | no init in `app-frontend`; telemetry opt-in → `api.owyx.site` |
| Header Download/ЛК | documented `max-w-6xl` + `max-xl:pr-40` |
| Cosmetics `/me` | login `nickname`, not display nick |
| Friends list LIMIT | 500 |
| Password complexity | shared helper on register / reset / profile |
| Admin ban + role=admin | protected |
| `publicBase` | env only, no `Host` fallback |
| Owyx GitHub Release `v0.10.0` | **success** ([35923520077](https://github.com/ebluffy/Owyx/actions/runs/35923520077)) |
| theseus-build / theseus-release | skipped (`github.repository == 'modrinth/code'`) |

---

## 4. Not filed again (still deferred on purpose)

Same class as #133 “Not filed”:

- Website JWT in browser `localStorage` (SPA; launcher uses OS file)
- Friends schema via `ensureFriendsSchema` vs missing `015_*.sql` migration file
- Plugin-era `api_tokens.permissions` **until this audit** — now filed as **H** because `/tokens` UI implies scopes
- Socket.IO / Discord-start JWT in query — **now filed as C** (landmine + morgan)
- In-memory IP buckets (single-instance; noted under G/I)
- `jwt.verify` without explicit `algorithms: ['HS256']` (defense in depth)
- Mass-rename `@modrinth/*`
- `apps/frontend` / `apps/labrinth` / `packages/ui` Hosting stories
- Catalog “Open in Modrinth” / CSL Modrinth mod URL (third-party, contract-allowed)
- Unmounted `SurveyPopup` / BrandPalette
- Login min 6 vs register 8
- Crowdin itself (skip the job; don’t add secrets in this PR)
- Header `max-xl:pr-40` vs footer (documented)
- In-game presence TTL ~90s after crash (server already expires)
- Admin publish orphan pack if ingest fails (admin-only)
- `filterByAcl` guests see `open` only (documented product rule)

---

## 5. Could not fully verify

- Header overlap pixels / avatar crop vs Sharp (no Next/browser in this pass)
- Production SMTP / Turnstile / Discord OAuth / Mailjet on VPS
- Whether prod already ran `purge-resource-id-telemetry.sql`
- Live `GET https://owyx.site/uploads/packs/…` cache headers (source + `deploy/nginx-owyx.site.conf` only)
- Packaged `.exe` network (Sentry/PostHog) — source shows init removed
- turbo-ci lint on `1f421de75` (job died at Docker)
- Local `cargo clippy` in this agent VM (missing GTK/GDK)

---

## 6. Suggested fix order

1. **D** [#146](https://github.com/ebluffy/Owyx/issues/146) — unblock fork CI (otherwise every push is a coin flip).
2. **A** [#143](https://github.com/ebluffy/Owyx/issues/143) + **B** [#144](https://github.com/ebluffy/Owyx/issues/144) — user-visible Servers / skins holes from #127–#129 leftovers.
3. **C** [#145](https://github.com/ebluffy/Owyx/issues/145) — remove query JWT before chat/Discord UI ships.
4. **E** [#147](https://github.com/ebluffy/Owyx/issues/147) — one-liner clipboard.
5. **G / H / I** [#149](https://github.com/ebluffy/Owyx/issues/149) / [#150](https://github.com/ebluffy/Owyx/issues/150) / [#151](https://github.com/ebluffy/Owyx/issues/151) — auth hardening.
6. **K / L / J** [#153](https://github.com/ebluffy/Owyx/issues/153) / [#154](https://github.com/ebluffy/Owyx/issues/154) / [#152](https://github.com/ebluffy/Owyx/issues/152) — CI + i18n hygiene.
7. Upstream launcher sync is tracked in this working sync PR using [`UPSTREAM_SYNC.md`](./UPSTREAM_SYNC.md). Keep remaining audit fixes after the upstream commits in separate commits, then re-run Auto-Review.
